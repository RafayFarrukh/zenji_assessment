# Absolute origin of the deployment. Used for canonical URLs, the sitemap,
# JSON-LD and Open Graph tags. Falls back to the Vercel preview domain.
NEXT_PUBLIC_SITE_URL=https://your-deployment.vercel.app
